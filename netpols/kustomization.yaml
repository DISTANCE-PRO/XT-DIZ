apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# Egress must be explicitly allowed, as our default policy denies all.
- egress.prod-lb.allow.yaml
- egress.trust-center.allow.yaml
