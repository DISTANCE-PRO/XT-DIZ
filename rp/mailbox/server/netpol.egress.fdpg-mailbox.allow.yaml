apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rp-mailbox-allow-fdpg-mailbox
spec:
  podSelector:
    matchLabels: {}
  policyTypes: [ Egress ]
  egress:
  # access fdpg mailbox
  - to:
    - ipBlock:
        cidr: 139.18.82.225/32 # public-lb
    ports:
    - port: 443
